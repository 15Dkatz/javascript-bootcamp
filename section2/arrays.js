var things = ['laptop', 'speaker', 13];

things.length;

// Add an item to the array
things.push(17);

// .splice(n, x, ...)
// Starting from index n, remove x items.
// Add additional arguments to the array
things.splice(1, 0, 'pen', 'paper');

